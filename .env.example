# Copy to .env (root) if you want to standardize values across services.
# Individual services also have their own .env files.

WEB_ORIGIN=http://localhost:3000
DATABASE_URL=postgresql://dispatch:dispatch@localhost:5432/dispatch
JWT_SECRET=change_me_change_me_change_me
