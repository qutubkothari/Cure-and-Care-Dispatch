# Copy to .env.ec2 on the EC2 instance and set real values.

# API
JWT_SECRET=change_me_change_me_change_me

# Web allowed origin for CORS (set to your domain later)
WEB_ORIGIN=http://localhost:3000

# Optional: if you later move DB off-instance, set DATABASE_URL accordingly.
# For docker-compose.ec2.yml we default to the in-house Postgres container.
